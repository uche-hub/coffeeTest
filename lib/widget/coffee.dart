import 'package:coffeetest/styles/styles.dart';
import 'package:division/division.dart';
import 'package:flutter/material.dart';

class CoffeeBox extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Parent(
      style: parentStyle,
    );
  }
}
